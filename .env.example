DATABASE_URL="file:./dev.db"
AUTH_SECRET="replace-with-a-long-random-string"
# Comma-separated browser origins. Leave empty until Sam chooses the production site.
# Development already allows http://localhost and http://127.0.0.1. A wildcard is ignored in production.
CORS_ORIGINS=""
# Optional directory for private customer PDFs. Defaults to storage/private.
# FILE_STORAGE_DIR=""
